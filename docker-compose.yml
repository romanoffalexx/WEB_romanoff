services:
  web:
    build: .
    container_name: romanoff-portfolio
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
