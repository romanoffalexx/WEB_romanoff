services:
  web:
    build: .
    container_name: romanoff-portfolio
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
    name: dokploy-network
